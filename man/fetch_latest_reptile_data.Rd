% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_latest_reptile_data.R
\name{fetch_latest_reptile_data}
\alias{fetch_latest_reptile_data}
\title{Download and Process the Latest Reptile Database Checklist}
\usage{
fetch_latest_reptile_data(
  cache = TRUE,
  force = FALSE,
  cache_dir = NULL,
  silent = FALSE
)
}
\arguments{
\item{cache}{Logical. If \code{TRUE} (default), caches the processed dataset locally
so subsequent calls do not re-download the same dataset version.}

\item{force}{Logical. If \code{TRUE}, forces re-downloading and re-processing
even if a cached version already exists. Default is \code{FALSE}.}

\item{cache_dir}{Character string specifying custom cache directory. If \code{NULL}
(default), uses \code{tools::R_user_dir("reptiledb.data", "cache")}.}

\item{silent}{Logical. If \code{TRUE}, suppresses informative console messages.
Default is \code{FALSE}.}
}
\value{
A cleaned tibble with taxonomic and nomenclatural information.
}
\description{
Automatically checks for the latest available checklist dataset on
\href{http://www.reptile-database.org/}{The Reptile Database}, downloads the
raw Excel file, cleans and processes it using \code{\link{clean_reptile_data}},
and caches the result for fast future retrieval.
}
\examples{
\donttest{
# Fetch the latest dataset online
reptile_data <- fetch_latest_reptile_data()
head(reptile_data)
}

}
